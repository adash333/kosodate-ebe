# 用語解説イラスト追加

- 日付: 2026-06-12
- 連番: 02

## プロンプト

> 用語解説の記事のうち、画像がまだない記事に、他の用語解説記事と同じような雰囲気の明るい科学的なイラストを作成し、それぞれの記事に挿入する。以後は毎回 main に push し、今回も push 後に /save-prompt を行う。

## 概要

用語解説の未設定記事9本に、明るめの科学図解テイストのヒーロー画像を追加した。

## 実施内容

- `public/glossary/` に以下の9枚の画像を追加。
  - `default-mode-network.png`
  - `attachment.png`
  - `exposure.png`
  - `technoference.png`
  - `modeling.png`
  - `still-face.png`
  - `critical-period.png`
  - `self-control.png`
  - `hininchi-nouryoku.png`
- `src/glossary.ts` の対象9記事に `heroImage` と日本語 alt を追加。
- 対象記事:
  - デフォルトモードネットワーク
  - 愛着（アタッチメント）・安全基地
  - エクスポージャー（段階的曝露）
  - テクノフェランス
  - モデリング（観察学習）
  - スティルフェイス実験
  - 臨界期・敏感期
  - 自制心（マシュマロ実験）
  - 非認知能力

## 検証

- `npm run build` 成功。
- Vite preview 上で9画像すべてが `200 image/png` で返ることを確認。
- ビルド済み JS に各 `/glossary/*.png` 参照が含まれることを確認。
- `git diff --check` 成功。

## 反映

- 実装コミット: `007f9df Add glossary term illustrations`
- `git push origin main` で GitHub の `main` に反映済み。
- この作業ログも追加コミットとして `main` に push する。

## 補足

今後このリポジトリでは、ユーザーから別指示がない限り、作業完了時に `main` へ push する前提で進める。
