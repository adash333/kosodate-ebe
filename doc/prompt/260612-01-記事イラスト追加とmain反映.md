# 記事イラスト追加と main 反映

- 日付: 2026-06-12
- 連番: 01

## プロンプト

> 子どもの好き嫌い・偏食 — 「無理に食べさせない」が近道なわけ 体を動かすと頭も育つ — 運動・外遊びと学力・集中力のエビデンス
>
> の記事に、
>
> 「すごい」より効く、子どものほめ方 — 研究でわかった3つのコツ
>
> と同じように、上の方にイラストを追加して下さい

> mainにpushして下さい

> スキル　/save-prompt をお願いします。全てpushして下さい

## 概要

読み物記事のうち、`henshoku` と `undou-nou` の2記事に、既存の「ほめ方」記事と同じ `heroImage` 形式で上部イラストを追加した。

## 実施内容

- `git pull` で `origin/main` の最新記事追加分を取り込み。
- 画像生成機能で記事上部用の横長イラストを2枚作成。
- 生成画像を以下に配置。
  - `public/articles/henshoku.png`
  - `public/articles/undou-nou.png`
- `src/articles.ts` の対象2記事に `heroImage` を追加。
  - `子どもの好き嫌い・偏食 — 「無理に食べさせない」が近道なわけ`
  - `体を動かすと頭も育つ — 運動・外遊びと学力・集中力のエビデンス`

## 検証

- `npm run build` が成功。
- Vite preview を `http://127.0.0.1:4173` で起動し、以下のページで画像読み込みを確認。
  - `/articles/henshoku`
  - `/articles/undou-nou`
- ブラウザ確認では、両画像とも `1672x941` の PNG として読み込まれていた。

## 反映

- コミット: `99df8ae Add illustrations for new articles`
- `origin/main` へ push 済み。

## 補足

`/save-prompt` のスキル定義ファイルはセッション上のスキル一覧およびユーザーホーム配下では見つからなかったため、既存の `doc/prompt/` 保存ルールに従ってこの記録を作成した。
